# Release

1. Mettre a jour `addon/VERSION`.
2. Verifier la version runtime dans `addon/dist/custom_js.html` si utilisee.
3. Commit puis tag `vX.Y.Z`.
4. Push du tag.

Le workflow publie:
- `dist/nerdcore-addon-vX.Y.Z.zip`
